import React,{useRef,useState}from"react";import{APPS}from"./catalog.js";export default function Terminal(){const[lines,setLines]=useState(["Last login: today on makOS","makOS Unix shell 1.0","Type 'help' for commands."]);const[input,setInput]=useState("");const ref=useRef();const run=cmd=>{const c=cmd.trim();if(!c)return;if(c==="clear"){setLines([]);return}let o=c==="help"?"help  ls  pwd  whoami  date  uname  echo  clear  neofetch  apps  open <app>":c==="ls"?"Applications  Desktop  Documents  Downloads  Library  Projects":c==="pwd"?"/Users/mako":c==="whoami"?"mako":c==="uname"?"makOS_Tahoe arm64 Unix":c==="date"?new Date().toString():c==="neofetch"?"M  makOS Tahoe\n   React + Vite / Cloudflare Pages\n   shell: makosh":c==="apps"?Object.keys(APPS).join("  "):c.startsWith("echo ")?c.slice(5):c.startsWith("open ")?("opening "+c.slice(5)+"..."):"zsh: command not found: "+c;setLines(l=>[...l,"$ "+c,o])};return <div className="terminal" onClick={()=>ref.current?.focus()}><div className="termHeader"><span>makOS Unix</span><span>zsh • 1.0</span></div><div className="termLines">{lines.map((l,i)=><div key={i}>{l}</div>)}<div className="prompt"><span>mako@makOS ~ %</span><input ref={ref} value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>{if(e.key==="Enter"){run(input);setInput("")}}}/></div></div></div>}