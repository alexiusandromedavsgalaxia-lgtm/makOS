import React,{useRef,useState}from"react";
import{loadFS,saveFS,normalizePath,children,mkdir,touch,remove,readFile}from"../system/filesystem.js";
export default function Terminal(){const[lines,setLines]=useState(["Last login: today on makOS","makOS Unix shell 1.0","Type 'help' for commands."]),[input,setInput]=useState(""),[cwd,setCwd]=useState("/Users/mako"),ref=useRef();
 const run=raw=>{const c=raw.trim();if(!c)return;if(c==="clear"){setLines([]);return}const[a,...rest]=c.split(" "),arg=rest.join(" ");let fs=loadFS(),o="";
 try{switch(a){case"help":o="ls cd pwd mkdir touch cat rm rmdir clear echo whoami uname date neofetch apps open";break;case"pwd":o=cwd;break;case"ls":o=children(fs,cwd).map(x=>x.name+(x.type==="dir"?"/":"")).join("  ")||"(empty)";break;case"cd":{const p=normalizePath(arg||"/Users/mako",cwd);if(!fs[p]||fs[p].type!=="dir")throw Error("cd: no such directory");setCwd(p);break}case"mkdir":mkdir(fs,normalizePath(arg,cwd));saveFS(fs);o="created "+normalizePath(arg,cwd);break;case"touch":touch(fs,normalizePath(arg,cwd),"");saveFS(fs);o="created "+normalizePath(arg,cwd);break;case"cat":o=readFile(fs,normalizePath(arg,cwd));break;case"rm":remove(fs,normalizePath(arg,cwd));saveFS(fs);o="removed "+normalizePath(arg,cwd);break;case"rmdir":remove(fs,normalizePath(arg,cwd));saveFS(fs);o="removed "+normalizePath(arg,cwd);break;case"whoami":o="mako";break;case"uname":o="makOS_Tahoe arm64 browser-unix";break;case"date":o=new Date().toString();break;case"echo":o=arg;break;case"neofetch":o="      M   makOS Tahoe\n   React   Vite + Cloudflare Pages\n  kernel   makKernel 1.0\n  shell    makosh 1.0\n  fs       makFS";break;case"apps":o="Finder Safari Terminal Settings Calendar Notes Calculator Music Photos System";break;case"open":o="opening "+arg+"…";break;default:o="makosh: command not found: "+a}}catch(e){o=e.message}setLines(l=>[...l,"mako@makOS "+cwd+" % "+c,...(o?o.split("\n"):[])]);};
 return <div className="terminal" onClick={()=>ref.current?.focus()}><div className="termHeader"><span>makOS Unix</span><span>makosh • {cwd}</span></div><div className="termLines">{lines.map((l,i)=><div key={i}>{l}</div>)}<div className="prompt"><span>mako@makOS %</span><input ref={ref} value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>{if(e.key==="Enter"){run(input);setInput("")}}}/></div></div></div>}