import React,{useState}from"react";import{ChevronLeft,ChevronRight,Plus,Trash2}from"lucide-react";
const KEY="makOS.calendar.v2";export default function Calendar(){const now=new Date(),[month,setMonth]=useState(new Date(now.getFullYear(),now.getMonth(),1)),[selected,setSelected]=useState(now.getDate()),[events,setEvents]=useState(()=>{try{return JSON.parse(localStorage.getItem(KEY))||{}}catch{return{}}});const key=d=>d.toISOString().slice(0,10);const days=new Date(month.getFullYear(),month.getMonth()+1,0).getDate(),start=new Date(month.getFullYear(),month.getMonth(),1).getDay();const sel=new Date(month.getFullYear(),month.getMonth(),selected),k=key(sel),add=()=>{const title=prompt("Event title");if(!title)return;const n={...events,[k]:[...(events[k]||[]),title]};setEvents(n);localStorage.setItem(KEY,JSON.stringify(n))};const del=()=>{const a=[...(events[k]||[])];a.pop();const n={...events,[k]:a};setEvents(n);localStorage.setItem(KEY,JSON.stringify(n))};return <div className="calendar"><div className="calHead"><button onClick={()=>setMonth(new Date(month.getFullYear(),month.getMonth()-1,1))}><ChevronLeft/></button><h1>{month.toLocaleString([],{month:"long",year:"numeric"})}</h1><button onClick={()=>setMonth(new Date(month.getFullYear(),month.getMonth()+1,1))}><ChevronRight/></button><button onClick={()=>{setMonth(new Date(now.getFullYear(),now.getMonth(),1));setSelected(now.getDate())}}>Today</button></div><div className="week">{["Sun","Mon","Tue","Wed","Thu","Fri","Sat"].map(x=><b key={x}>{x}</b>)}{Array.from({length:start},(_,i)=><span className="muted" key={"e"+i}/>) }{Array.from({length:days},(_,i)=>{const d=i+1;const dd=new Date(month.getFullYear(),month.getMonth(),d);return <button className={d===selected?"day selected":"day"} key={d} onClick={()=>setSelected(d)}>{d}{events[key(dd)]?.length?<i/>:null}</button>})}</div><div className="calEvents"><b>{sel.toLocaleDateString([], {weekday:"long",day:"numeric",month:"long"})}</b>{(events[k]||[]).map((e,i)=><div key={i}>{e}</div>)}<button onClick={add}><Plus/> Add event</button><button onClick={del} disabled={!events[k]?.length}><Trash2/></button></div></div>}